# 🔗 Link Shortener

---

## Setup
Create file `.env`:
```dotenv
SECRET_KEY=your-secret-key
ALGORITHM=HS256
DATABASE_URL=sqlite:///Base.db
```
then download the libraries and run:
```shell
$ pip install -r requirements.txt
$ python3 main.py
```